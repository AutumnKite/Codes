#include <string>
#include <iostream>
#include <vector>
#include <algorithm>
#include <random>
#include <functional>
using std::pair;
using std::make_pair;
using std::function;
using std::vector;
using std::string;
using std::cout;
using std::endl;
using std::min;
using std::max;
using std::swap;

vector<int> solve(int, int);
bool query1(int, int);
int query2(int, int, int);
