#include <bits/stdc++.h>

int main() {
	std::ios_base::sync_with_stdio(false);
	std::cin.tie(0);

	int n, m, q;
	std::cin >> n >> m >> q;
	std::vector<std::set<int>> Sl(n), Sr(n);
	while (q--) {
		int x, y;
		std::cin >> x >> y;
		--x, --y;
		if ()
	}
}