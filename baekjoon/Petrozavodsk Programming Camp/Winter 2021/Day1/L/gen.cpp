#include <bits/stdc++.h>

std::mt19937 rnd(time(0));

int main() {
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(nullptr);

  int T = 100;
  std::cin >> T;
  while (T--) {
    int x = rnd() % 11 - 5, y = rnd() % 11 - 5;
    std::cout << x << " " << y << "\n";
    int 
  }
}
